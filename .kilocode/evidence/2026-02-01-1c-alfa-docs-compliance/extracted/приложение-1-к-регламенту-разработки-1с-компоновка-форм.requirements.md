# Extracted requirements: Приложение №1 к регламенту разработки 1С. Компоновка форм.docx

⚠️ Это промежуточный артефакт извлечения (не firm-SoT).
Используется для последующего переноса требований в стабильные источники.

Source (read-only): temp/Документация нашей фирмы/… (без ссылки)

# 1. Общие принципы

## 1.1. Внутри одной формы все заголовки для однострочных полей ввода, переключателей, тумблеров следует располагать одинаково – или слева, или сверху.

- [SHOULD] 1.1. Внутри одной формы все заголовки для однострочных полей ввода, переключателей, тумблеров следует располагать одинаково – или слева, или сверху. (section: 1. Общие принципы > 1.1. Внутри одной формы все заголовки для однострочных полей ввода, переключателей, тумблеров следует располагать одинаково – или слева, или сверху.)
- [SHOULD] Заголовки многостраничных полей рекомендуется располагать в зависимости от общей композиции формы. (section: 1. Общие принципы > 1.1. Внутри одной формы все заголовки для однострочных полей ввода, переключателей, тумблеров следует располагать одинаково – или слева, или сверху.)

### 1.1.1. Если в группе однострочных полей заголовок одного поля существенно длиннее остальных заголовков, рекомендуется сократить название заголовка этого поля или переносить часть такого заголовка на другие строки.

- [SHOULD] 1.1.1. Если в группе однострочных полей заголовок одного поля существенно длиннее остальных заголовков, рекомендуется сократить название заголовка этого поля или переносить часть такого заголовка на другие строки. (section: 1. Общие принципы > 1.1. Внутри одной формы все заголовки для однострочных полей ввода, переключателей, тумблеров следует располагать одинаково – или слева, или сверху. > 1.1.1. Если в группе однострочных полей заголовок одного поля существенно длиннее остальных заголовков, рекомендуется сократить название заголовка этого поля или переносить часть такого заголовка на другие строки.)

### 1.1.2. Поля взаимосвязанных реквизитов, находящиеся в одной группе, должны выравниваться по опорной линии.

- [MUST] 1.1.2. Поля взаимосвязанных реквизитов, находящиеся в одной группе, должны выравниваться по опорной линии. (section: 1. Общие принципы > 1.1. Внутри одной формы все заголовки для однострочных полей ввода, переключателей, тумблеров следует располагать одинаково – или слева, или сверху. > 1.1.2. Поля взаимосвязанных реквизитов, находящиеся в одной группе, должны выравниваться по опорной линии.)

## 1.2. Элементы, которые в разных формах выполняют одну и ту же функцию, следует называть одинаково и всегда выводить примерно в одном и том же месте.

- [SHOULD] 1.2. Элементы, которые в разных формах выполняют одну и ту же функцию, следует называть одинаково и всегда выводить примерно в одном и том же месте. (section: 1. Общие принципы > 1.2. Элементы, которые в разных формах выполняют одну и ту же функцию, следует называть одинаково и всегда выводить примерно в одном и том же месте.)

## 1.3. Если один из элементов интерфейса влияет на другие элементы, то он должен быть расположен до этих элементов (выше или левее, в зависимости от ситуации). При работе с формой пользователь сначала должен выполнить действия, связанные с основным элементом, а потом уже перейти к работе с зависимыми.

- [MUST] Аналогично, если внутри формы есть взаимосвязанные табличные части, то они должны располагаться рядом друг с другом. (section: 1. Общие принципы > 1.3. Если один из элементов интерфейса влияет на другие элементы, то он должен быть расположен до этих элементов (выше или левее, в зависимости от ситуации). При работе с формой пользователь сначала должен выполнить действия, связанные с основным элементом, а потом уже перейти к работе с зависимыми.)

### 1.3.1. Использование вертикальной зеленой черты для выделенных отдельных элементов и групп взаимосвязанных реквизитов не допускается.

- [MUST] 1.3.1. Использование вертикальной зеленой черты для выделенных отдельных элементов и групп взаимосвязанных реквизитов не допускается. (section: 1. Общие принципы > 1.3. Если один из элементов интерфейса влияет на другие элементы, то он должен быть расположен до этих элементов (выше или левее, в зависимости от ситуации). При работе с формой пользователь сначала должен выполнить действия, связанные с основным элементом, а потом уже перейти к работе с зависимыми. > 1.3.1. Использование вертикальной зеленой черты для выделенных отдельных элементов и групп взаимосвязанных реквизитов не допускается.)
- [SHOULD] В качестве выделения следует использовать состояния: (section: 1. Общие принципы > 1.3. Если один из элементов интерфейса влияет на другие элементы, то он должен быть расположен до этих элементов (выше или левее, в зависимости от ситуации). При работе с формой пользователь сначала должен выполнить действия, связанные с основным элементом, а потом уже перейти к работе с зависимыми. > 1.3.1. Использование вертикальной зеленой черты для выделенных отдельных элементов и групп взаимосвязанных реквизитов не допускается.)

## 1.4. Внутри формы документа рекомендуется группировать элементы следующим образом:   В левой и правой колонках шапки следует располагать реквизиты, необходимые для правильного отражения документа в программе, при этом следует распределять реквизиты по следующим признакам:

- [SHOULD] 1.4. Внутри формы документа рекомендуется группировать элементы следующим образом:   В левой и правой колонках шапки следует располагать реквизиты, необходимые для правильного отражения документа в программе, при этом следует распределять реквизиты по следующим признакам: (section: 1. Общие принципы > 1.4. Внутри формы документа рекомендуется группировать элементы следующим образом:   В левой и правой колонках шапки следует располагать реквизиты, необходимые для правильного отражения документа в программе, при этом следует распределять реквизиты по следующим признакам:)
- [MUST] - обязательны для заполнения (section: 1. Общие принципы > 1.4. Внутри формы документа рекомендуется группировать элементы следующим образом:   В левой и правой колонках шапки следует располагать реквизиты, необходимые для правильного отражения документа в программе, при этом следует распределять реквизиты по следующим признакам:)
- [MUST] - не обязательны для заполнения (section: 1. Общие принципы > 1.4. Внутри формы документа рекомендуется группировать элементы следующим образом:   В левой и правой колонках шапки следует располагать реквизиты, необходимые для правильного отражения документа в программе, при этом следует распределять реквизиты по следующим признакам:)

### 1.4.1. При компоновке документов, содержащих табличную часть, следует ориентироваться на видимое количество строк в табличной части. Табличная часть документа должна содержать не менее 7-ми строк (оптимально — не менее 10-ти строк). Если количество строк в табличной части менее 7-ми — рекомендуется размещать табличную часть на отдельной вкладке.Например, в документе "Отчет комитенту" реквизиты шапки размещены во вкладке с основными реквизитам. Таким образом, количество строк таблицы вкладки "Товары" реквизитами не ограничивается:

- [MUST] 1.4.1. При компоновке документов, содержащих табличную часть, следует ориентироваться на видимое количество строк в табличной части. Табличная часть документа должна содержать не менее 7-ми строк (оптимально — не менее 10-ти строк). Если количество строк в табличной части менее 7-ми — рекомендуется размещать табличную часть на отдельной вкладке.Например, в документе "Отчет комитенту" реквизиты шапки размещены во вкладке с основными реквизитам. Таким образом, количество строк таблицы вкладки "Товары" реквизитами не ограничивается: (section: 1. Общие принципы > 1.4. Внутри формы документа рекомендуется группировать элементы следующим образом:   В левой и правой колонках шапки следует располагать реквизиты, необходимые для правильного отражения документа в программе, при этом следует распределять реквизиты по следующим признакам: > 1.4.1. При компоновке документов, содержащих табличную часть, следует ориентироваться на видимое количество строк в табличной части. Табличная часть документа должна содержать не менее 7-ми строк (оптимально — не менее 10-ти строк). Если количество строк в табличной части менее 7-ми — рекомендуется размещать табличную часть на отдельной вкладке.Например, в документе "Отчет комитенту" реквизиты шапки размещены во вкладке с основными реквизитам. Таким образом, количество строк таблицы вкладки "Товары" реквизитами не ограничивается:)

### 1.4.2. Если на форме имеются несколько невзаимосвязанных табличных частей, то их следует размещать на отдельных вкладках.

- [SHOULD] 1.4.2. Если на форме имеются несколько невзаимосвязанных табличных частей, то их следует размещать на отдельных вкладках. (section: 1. Общие принципы > 1.4. Внутри формы документа рекомендуется группировать элементы следующим образом:   В левой и правой колонках шапки следует располагать реквизиты, необходимые для правильного отражения документа в программе, при этом следует распределять реквизиты по следующим признакам: > 1.4.2. Если на форме имеются несколько невзаимосвязанных табличных частей, то их следует размещать на отдельных вкладках.)

## 1.5. Формы следует проектировать таким образом, чтобы они не были перегружены функциями, реквизитами и элементами.

- [SHOULD] 1.5. Формы следует проектировать таким образом, чтобы они не были перегружены функциями, реквизитами и элементами. (section: 1. Общие принципы > 1.5. Формы следует проектировать таким образом, чтобы они не были перегружены функциями, реквизитами и элементами.)

# 2. Вкладки

## 2.1. Название вкладки должно отражать суть того, что на ней располагается. Например, по названию вкладки "Товары" понятно, что на ней отображается список товаров:

- [MUST] 2.1. Название вкладки должно отражать суть того, что на ней располагается. Например, по названию вкладки "Товары" понятно, что на ней отображается список товаров: (section: 2. Вкладки > 2.1. Название вкладки должно отражать суть того, что на ней располагается. Например, по названию вкладки "Товары" понятно, что на ней отображается список товаров:)

## 2.2. Поля, обязательные для заполнения, следует располагать на вкладке, активной в момент открытия формы

- [MUST] 2.2. Поля, обязательные для заполнения, следует располагать на вкладке, активной в момент открытия формы (section: 2. Вкладки > 2.2. Поля, обязательные для заполнения, следует располагать на вкладке, активной в момент открытия формы)

## 2.3. Не рекомендуется делать вкладки, содержащие небольшое число элементов (1-2), если они не занимают все пространство вкладки

- [SHOULD] 2.3. Не рекомендуется делать вкладки, содержащие небольшое число элементов (1-2), если они не занимают все пространство вкладки (section: 2. Вкладки > 2.3. Не рекомендуется делать вкладки, содержащие небольшое число элементов (1-2), если они не занимают все пространство вкладки)

# 3. Вспомогательные формы

## 3.1. При размещении элементов во вспомогательных формах рекомендуется действовать следующим образом:

- [SHOULD] 3.1. При размещении элементов во вспомогательных формах рекомендуется действовать следующим образом: (section: 3. Вспомогательные формы > 3.1. При размещении элементов во вспомогательных формах рекомендуется действовать следующим образом:)

# 1. Среди всего множества реквизитов и элементов выбрать второстепенные, в большинстве случаев заполняемые автоматически, а также те, которые использует небольшое количество пользователей. Чаще всего такие реквизиты будут являться необязательными для заполнения

# 2. Разделить эти элементы на несколько смысловых групп

# 3. Разместить эти группы в одной или нескольких вспомогательных формах

## 3.2. Для открытия вспомогательной формы следует использовать гиперссылку, размещаемую в основной форме. При открытии вспомогательная форма блокирует окно владельца.

- [SHOULD] 3.2. Для открытия вспомогательной формы следует использовать гиперссылку, размещаемую в основной форме. При открытии вспомогательная форма блокирует окно владельца. (section: 3. Разместить эти группы в одной или нескольких вспомогательных формах > 3.2. Для открытия вспомогательной формы следует использовать гиперссылку, размещаемую в основной форме. При открытии вспомогательная форма блокирует окно владельца.)

## 3.3. Формирование текста гиперссылки.

- [MUST] Вариант 1. В тексте ссылки указывается название вспомогательной формы. При этом название должно быть таким, чтобы по нему быть понятно, какие реквизиты могут располагаться внутри. (section: 3. Разместить эти группы в одной или нескольких вспомогательных формах > 3.3. Формирование текста гиперссылки.)
- [SHOULD] Вариант 2. В тексте ссылки указывается перечень значений реквизитов, заполняемых внутри вспомогательной формы. Если этот перечень слишком велик, то в первую очередь в него следует вынести реквизиты: (section: 3. Разместить эти группы в одной или нескольких вспомогательных формах > 3.3. Формирование текста гиперссылки.)
- [MUST] - которые обязательны для заполнения (section: 3. Разместить эти группы в одной или нескольких вспомогательных формах > 3.3. Формирование текста гиперссылки.)
- [SHOULD] Если текст ссылки выводится в виде перечня значений, то для нее следует добавлять заголовок. В табличной части заголовком будет являться название колонки. (section: 3. Разместить эти группы в одной или нескольких вспомогательных формах > 3.3. Формирование текста гиперссылки.)

## 3.4. Если внутри вспомогательной формы есть реквизиты, обязательные для заполнения, и они не заполнены, то для гиперссылки используется красный цвет (элемент стиля вида цвет "НезаполненныйРеквизит", RGB 178,34,34)

- [MUST] 3.4. Если внутри вспомогательной формы есть реквизиты, обязательные для заполнения, и они не заполнены, то для гиперссылки используется красный цвет (элемент стиля вида цвет "НезаполненныйРеквизит", RGB 178,34,34) (section: 3. Разместить эти группы в одной или нескольких вспомогательных формах > 3.4. Если внутри вспомогательной формы есть реквизиты, обязательные для заполнения, и они не заполнены, то для гиперссылки используется красный цвет (элемент стиля вида цвет "НезаполненныйРеквизит", RGB 178,34,34))

## 3.5. Командная панель во вспомогательной форме состоит из двух кнопок: "ОК" и "Отмена" и располагается в нижней части формы:

# 4. Свертываемые группы

## 4.1. Свертываемые группы являются разделами реквизитов или настроек, логически связанных между собой и объединенных общим заголовком. Возможность свертывания групп позволяет упростить поиск нужного раздела и информации, которая в нем расположена.

## 4.2. Формы, вся информация в которых размещена в свертываемых группах, рекомендуется отображать с учетом быстрого доступа к содержимому разделов:

- [SHOULD] 4.2. Формы, вся информация в которых размещена в свертываемых группах, рекомендуется отображать с учетом быстрого доступа к содержимому разделов: (section: 4. Свертываемые группы > 4.2. Формы, вся информация в которых размещена в свертываемых группах, рекомендуется отображать с учетом быстрого доступа к содержимому разделов:)
