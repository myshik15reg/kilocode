---
description: "Workflow: create a new protocol (concepts + structure + minimal templates)"
argument-hint: "protocol slug (kebab-case)"
mode: code
---

Source of Truth (workflow): [`../workflows/protocol-new.md`](../workflows/protocol-new.md:1)

This is the canonical workflow for creating a new protocol folder and filling `brief.md` and `plan.md`.

Язык и кодировка: новые протоколы по умолчанию ведутся на русском и должны сохраняться в UTF-8 без BOM. См. [`../rules/language-and-encoding.md`](../rules/language-and-encoding.md:1).
