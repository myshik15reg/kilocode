---
description: "Scaffold a new protocol folder under .protocols/"
argument-hint: "task-name (kebab-case) [+ optional UZ number]"
mode: code
---

Source of Truth (workflow): [`../workflows/init-protocol.md`](../workflows/init-protocol.md:1)

Use this command to scaffold a new protocol folder under [`../../.protocols/`](../../.protocols/:1) and create the standard files (`brief.md`, `plan.md`, `artifacts/`).

Язык и кодировка: новые файлы протокола по умолчанию пишутся на русском и сохраняются в UTF-8 без BOM. См. [`../rules/language-and-encoding.md`](../rules/language-and-encoding.md:1).
