{
	"version": 1,
	"description": "Configure commands for your repo. The runner executes steps sequentially and fails fast on the first non-zero exit code.",
	"steps": [
		{
			"name": "Guardrails",
			"command": "pwsh",
			"args": ["-File", "./scripts/workflowai-guardrails.ps1", "-ProjectPath", ".", "-CheckKiloCodeChangeMarkers"]
		}
	]
}
