/**
 * Простая функция для тестирования базовой индексации
 */
export function calculateSum(a: number, b: number): number {
  return a + b;
}

export function calculateProduct(a: number, b: number): number {
  return a * b;
}