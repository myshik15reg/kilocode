version: '3.8'

services:
  neo4j:
    image: neo4j:5.15-community
    container_name: kilocode-neo4j
    restart: unless-stopped
    ports:
      - "7474:7474"  # HTTP
      - "7687:7687"  # Bolt
    environment:
      # Учетные данные по умолчанию для разработки
      NEO4J_AUTH: neo4j/kilocode123
      # Позволяет загружать данные из любого места (только для разработки)
      NEO4J_dbms_security_procedures_unrestricted: apoc.*
      # Включаем APOC плагины для расширенных функций
      NEO4J_dbms_security_procedures_allowlist: apoc.*
      # Увеличиваем лимит памяти для обработки больших графов
      NEO4J_dbms_memory_heap_initial__size: 512m
      NEO4J_dbms_memory_heap_max__size: 2G
      NEO4J_dbms_memory_pagecache_size: 1G
    volumes:
      - neo4j_data:/data
      - neo4j_logs:/logs
      - neo4j_import:/var/lib/neo4j/import
      - neo4j_plugins:/plugins
    networks:
      - kilocode-network

volumes:
  neo4j_data:
  neo4j_logs:
  neo4j_import:
  neo4j_plugins:

networks:
  kilocode-network:
    driver: bridge